# Handover

Per-session pickup notes live in [`context/`](./context/) as dated files (`handover-YYYY-MM-DD-HHMM.md`).

The `/handover` skill auto-detects the folder and writes a new dated file each session — no manual rotation, full history retained.

**Most recent:** [`context/handover-2026-05-31-1703.md`](./context/handover-2026-05-31-1703.md) — Komoot-class `/map` redesign: M3b (Vermilion selected pin + map↔list scroll sync) and M3 (two-tier pins: dots zoomed out, branded icons zoomed in) shipped and merged to master. M4 (consolidate the in-context detail components) next.
