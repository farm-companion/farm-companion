# Handover — 2026-05-17 00:16

> Branch: `claude/add-sitemap-page-wHEV4` · 1 commit this session (local only) · ⚠️ not pushed — rebase pending

## Mission
Begin Stage 0 batch conductor work per prior session's HANDOVER: declutter docs root, scaffold Docker deployment for Coolify (Dockerfile, .dockerignore, docker-compose.dev.yml, Next.js standalone output), kill lockfile drift. Mid-session pivot: forensic audit of installed Claude Code skills/plugins for malware.

## What landed
Stage 0 housekeeping committed locally as `6d943a9` (NOT YET PUSHED at the time):
- 14 stale root markdowns → `docs/archive/`.
- `farm-frontend/Dockerfile` (Node 22 bookworm-slim, pnpm via corepack, standalone runtime, non-root user, HEALTHCHECK).
- `farm-frontend/.dockerignore`.
- `docker-compose.dev.yml` (postgres+postgis 16-3.4, redis 7, meilisearch v1.10).
- `farm-frontend/next.config.ts` got `output: 'standalone'`.
- `.gitignore` deduplicated.
- `farm-frontend/package-lock.json` deleted (drift fix; pnpm canonical).
- Ledger appended.

## Verified
- File-level git status.
- Skill/plugin malware audit clean (0 bidi, 0 homoglyph, 1 benign ZWJ).

## NOT verified — user must do this
- ⏳ Push + rebase against remote.
- ⏳ Dockerfile build.
- ⏳ Dev infra health.
- ⏳ Standalone Next build.

## Decisions
- **Bookworm-slim base over alpine** — sharp/prisma native deps; rejected alpine pain.
- **Infra-only compose** — host pnpm dev is faster; rejected app-in-compose.
- **pnpm canonical** — no dual lockfiles; rejected keeping both.
- **Vercel strip is multi-slice, deferred** — Slice 6+; rejected one-shot rip.

## You are here → next step
Push + rebase the local `6d943a9` onto the remote; resolve `farm-frontend/package-lock.json` modify/delete conflict by accepting deletion; verify `next.config.ts` standalone setting survives auto-merge.
