# Handover — 2026-05-18 10:10

> Branch: `fix/kv-key-prefix` (now merged) · 3 PRs shipped this session · clean working tree

## Mission
Restore production-acceptable latency on `/api/farms` after the DigitalOcean → Hetzner DB migration left cold hits at ~10s. Three slices shipped covering region, KV-cache hangs, and KV multi-tenancy.

## What landed
- ✅ **Slice A — pin Vercel functions to `fra1`** — `vercel.json` (root + `farm-frontend/`); PR #144 → `b6b2075` on master.
- ✅ **Slice B — 200ms timeout Proxy on every `@upstash/redis` method** — `farm-frontend/src/lib/kv.ts` rewrite (Proxy wraps every method, throws typed `KvTimeoutError`) + new `farm-frontend/src/lib/kv.test.ts` (5 tests); PR #145 → `2e07587`.
- ✅ **Slice F — `KV_KEY_PREFIX` env var for multi-tenant Upstash safety** — `farm-frontend/src/lib/cache-manager.ts` (exported `prefixedNamespace` helper + private `nsKey()` applied at ALL 4 key-construction sites: `generateKey`, `set`-tag, `invalidateByTags`-tag, AND the critical `clearNamespace` scan pattern that would otherwise have been a cross-project deletion landmine) + new `farm-frontend/src/lib/cache-manager.test.ts` (8 tests); PR #146 → `6284c5b`.

## Verified
- **Slice A live:** `x-vercel-id: lhr1::fra1::…` confirmed via production curl post-merge.
- **Slice B live:** cold `/api/farms` dropped from ~10s → ~0.85s in production (12× faster); KV calls fail-open in 200ms instead of hanging 9s.
- **Slice B + F tests:** `pnpm exec tsx --test src/lib/{kv,cache-manager}.test.ts` → 5/5 + 8/8 pass; full suite (`pnpm test:unit`) no failures; `pnpm exec tsc --noEmit` exit 0.

## NOT verified — user must do this
- ⏳ **Slice F's prefix is actually being applied + Upstash cache writes are sticking in production.** After user reported merge + redeploy, three consecutive curls to `https://www.farmcompanion.co.uk/api/farms?county=<X>&_t=<N>` (different `N` each time, same county) all returned `x-cache: MISS`. The cache layer is still not holding writes. Verify by: Upstash console → your DB → **Metrics** tab → Commands chart over last 5–10 min.
  - Zero activity → env vars not reaching the function (most likely: vars not saved for **Production** scope, or no redeploy AFTER vars saved).
  - Non-zero activity → creds auth-failing (most likely: URL is `redis://…` RESP not `https://…upstash.io` REST; or token belongs to a different DB).

## Open threads
- ❓ Why `x-cache: MISS` persists post-Slice-F-merge — awaiting Upstash Metrics check.
- ⏳ **Slice B-followup (queued, ~30 LOC, 1 file + 2 routes):** `withPerformanceRateLimit` + `/api/contact/submit` + `/api/farms/submit` bypass the shim via `Redis.fromEnv()` and still hang ~9s if Upstash dead. Fix: import `kv` from shim + add `ephemeralCache: new Map()` to each `Ratelimit({...})` constructor.
- ⏳ **Slice C (queued):** carve `/api/farms` facet queries (county list, category list) into `unstable_cache` helpers with 1h TTL — drops cold further toward ~300ms even on app-cache miss.
- ⏳ **Slice D (queued):** `SELECT indexname FROM pg_indexes WHERE tablename = 'farms'` against live Hetzner DB; reconcile against `schema.prisma` `@@index` list; `pnpm prisma db push` if anything missing post-restore.
- ⚠️ **Carry-over from prior migration session:** enable Coolify automated backups on `farm-companion-db`; destroy DO droplet ~3-7 days after Hetzner verified stable (snapshot retained).

## Decisions
- **Option B (reuse Upstash + key prefix) over A/C/D/E**: zero monthly cost, no new infra, uses existing free-tier slot. Rejected: skip cache (A) — leaves rate-limit fail-open forever; delete-and-recreate (C) — destructive to other project; pay-as-you-go (D) — billing overhead for sub-$1/mo savings; `serverless-redis-http` on Coolify (E) — extra container to maintain, no upside vs Upstash for our scale.
- **Proxy-wrap every method (Slice B)** vs per-method explicit wrappers: future-proof against new `@upstash/redis` methods; covers cache-manager's full call set without enumeration; one place to tune timeout.
- **Prefix all 4 sites in lockstep (Slice F)** vs just data keys: the `clearNamespace` `kv.keys()` scan would have been a cross-project deletion landmine if left unprefixed. Council Critic seat already caught a similar write-path gap in Slice B; same instinct applied here.

## Council
Ran once during Slice B (Skeptic / Pragmatist / Critic; full transcript in execution-ledger Slice B entry). Verdict: ship A++ (read AND write path timeouts via Proxy). Critic's "read-only timeout is theater" framing forced the all-methods wrap over a narrower `kv.get`-only patch.

## You are here → next step
User opens Upstash console → Metrics chart; reports whether Commands count for the last 10 min is **zero** (→ env-var scope/redeploy problem) or **non-zero** (→ auth/URL problem). That single data point disambiguates the cache-MISS fix path.
