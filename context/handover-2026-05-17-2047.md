# Handover — 2026-05-17 20:47

> Branch: `perf/animatedstats-css-only` · 4 PRs this session (3 merged, 1 open) · working tree clean for this session's scope

## Mission
Continue post-Stage-0 perf and hygiene work — trim Lighthouse-flagged unused JS on production, ship small reviewable slices per CLAUDE.md queue rules.

## What landed
- ✅ **PR #138 merged** — sitemap.xml now emits proper `<sitemapindex>` (already opened pre-session; verified merged + production-deployed during this session). `d08e251`.
- ✅ **PR #139 merged** — dropped framer-motion from `PageTransition` (template-level, every route). Replaced motion.div+AnimatePresence with `key={pathname}` div + CSS `page-fade-enter` keyframe + `prefers-reduced-motion` override. `1fb8b16`. **This is the only slice with a real measurable user win — framer-motion is no longer in the shared template chunk.**
- ✅ **PR #140 merged** — removed dead framer-motion imports from `StatusBadge` and `Badge` (Badge's `animate` prop had zero callers). `f8474f3`. Cleanup only; bundle impact ~0 because Turbopack had already tree-shaken framer out of /shop, /counties, /find, /categories.
- ✅ **PR #141 OPEN** — converted `AnimatedStats` to CSS-only animations (reuses existing `.stagger-entry` utility + new `.stat-icon-hover` keyframe). `a61c050`. Step 1 of 4 toward removing framer-motion from `/`.

## Verified
- `pnpm build` passes for every slice — verified locally with fresh `.next/` rebuild.
- Framer-motion chunk distribution post PR #139: 3 chunks containing `AnimatePresence`, only loaded by `/` and `/compare`. Verified via `grep AnimatePresence .next/static/chunks/*.js`.
- `/shop`, `/counties/[slug]`, `/find/[county]/[category]`, `/categories/[slug]` load **zero** framer-motion chunks — confirmed via chunk-graph cross-reference against `.next/server/app/<route>.html`.
- The 113K core-js polyfill chunk only loads on legacy browsers via `<script nomodule>` — modern users don't pay for it. Verified via `grep noModule .next/server/app/index.html`.
- MapLibre correctly contained to `/map` (1MB chunk, not leaking).

## NOT verified — user must do this
- ⏳ PR #141 still open — review and merge: `gh pr view 141 --web`
- ⏳ Production Lighthouse rerun on www.farmcompanion.co.uk after PR #141 merges + Vercel redeploys, to confirm unused-JS budget actually dropped on homepage. Pre-#139 was 333 KiB unused JS / 61 mobile perf.
- ⏳ Vercel preview check on PR #141: stat cards stagger in, icons shake on hover, reduced-motion disables the shake.
- ⏳ Vercel env vars: `MAILBOXLAYER_API_KEY` + seven `S3_*`/`BLOB_*` for EV-1 email gateway and photo uploads. Operator-only task (carried from prior handover).

## Open threads
- ❓ **Local master still diverged**: 4 ahead, 39 behind origin/master. Backup tag at `94c5945` preserves the 4 orphaned commits (farm enrichment scripts). Decision pending from user — these were left untouched this session and I branched off `origin/master` directly for every PR to avoid the issue.
- ⚠️ **Homepage framer-motion: 3 of 4 components still pull it** — `AnimatedFeatures` (cinematic parallax via `useScroll`/`useTransform`), `NearbyFarms` (parallax + many `whileInView` reveals, 593-line file), `SeasonalShowcase` (`AnimatePresence mode="wait"` directional carousel). Each is an intentional brand animation that resists mechanical CSS replacement — needs visual review before conversion.
- ❌ **Investigated and ruled out**: the 113K "core-js polyfill" chunk is a red herring — `noModule` gated. Don't chase it.
- ❌ **Tried `git pull` on local master**: refused with "Not possible to fast-forward". Worked around by branching off `origin/master` directly.

## Decisions
- **Branch off `origin/master` for every slice, leave local master diverged.** Safe (backup tag at `94c5945`), avoids destructive reset without explicit user authorization.
- **Slice-by-slice framer-motion removal, not bulk LazyMotion migration.** Considered LazyMotion + `m` rewrite across 16 files; rejected as it exceeds CLAUDE.md's 8-file cap and provides no win until ALL files convert.
- **AnimatedStats conversion shipped despite ~0 immediate bundle impact** — frames it as Step 1/4 of a coordinated narrative toward homepage framer-motion removal. Reviewer can reject if cost-benefit unconvincing.

## You are here → next step
Wait for PR #141 review, then decide with the user whether to attempt `AnimatedFeatures` parallax-to-CSS conversion (uses `useScroll`/`useTransform`; conversion needs CSS `animation-timeline: view()` with IntersectionObserver fallback, and a visual sign-off before sinking the scope).
