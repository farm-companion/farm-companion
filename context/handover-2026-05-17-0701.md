# Handover — 2026-05-17 07:01

> Branch: `chore/dependabot-triage-2026-05-17` · 0 new commits this session · 2 files modified · ⚠️ uncommitted changes

## Mission
Decide Hetzner Object Storage bucket settings for the new `@vercel/blob` adapter (Slice 6d/e/f) and add a file-size rule to keep code digestible. A pre-existing ESLint crash surfaced mid-session and was diagnosed but not yet fixed.

## ⚠️ Uncommitted work
- `CLAUDE.md` — modified (+9 lines: new "File size rules" section)
- `docs/assistant/execution-ledger.md` — modified (+56 lines: Policy Slice A entry)
- `docs/PRODUCTION_READINESS_REPORT.md` and `.claude/` — untracked but pre-date this session, leave alone

**Branch surprise:** we landed on `chore/dependabot-triage-2026-05-17`, not `claude/add-sitemap-page-wHEV4` from the session-start gitStatus. The CLAUDE.md + ledger additions live on this branch. Decide if they belong here before committing.

## What landed
- ✅ File-size rule (soft 300 / hard 500 / forbidden 800 LOC; tests 2x; carve-outs listed) — `CLAUDE.md` under "Work unit rules"; **NOT YET COMMITTED**
- ✅ Slice entry "Policy Slice A: file-size rule" with calibration data (1,078 files scanned; mean 281; 197/74/27 above each tier), offender list, and full ESLint patch for paste-apply — `docs/assistant/execution-ledger.md`; **NOT YET COMMITTED**
- ❌ `farm-frontend/eslint.config.mjs` — **NOT WRITTEN.** `pre:edit-write:config-protection` hook blocked it (correctly — that hook routes lint-config changes through user consent). Patch content is in the ledger entry and was printed to the user in chat.

## Verified
- CLAUDE.md addition: `wc -l` 162 → 172; `grep -c "^## File size rules"` returns 1
- Ledger growth: `wc -l` 1052 → 1108 (+56 matches the slice entry)
- ESLint crash root cause: confirmed by inspecting `farm-frontend/node_modules/eslint-config-next/package.json` exports — `eslint-config-next@16.1.3` ships native flat configs at `/core-web-vitals` and `/typescript`. The current `FlatCompat.extends()` route triggers `@eslint/eslintrc@3.3.3`'s validator, which JSON-stringifies the React plugin and hits a circular reference.

## NOT verified — user must do this
- ⏳ Apply the ESLint patch — paste the 30-line file content (in the final chat message and the new ledger entry) into `farm-frontend/eslint.config.mjs`, OR start a Claude session with `ECC_DISABLED_HOOKS=pre:edit-write:config-protection` and let me write it.
- ⏳ Confirm the rule wires correctly — `cd farm-frontend && pnpm exec eslint src/lib/produce-image-generator.ts` should report exactly 1 `max-lines` warning (1053 LOC > 500).
- ⏳ Confirm the carve-out fires — same command on `src/data/best-lists.ts` (1445 LOC, carved) should report 0 `max-lines` warnings.
- ⏳ Confirm the ESLint crash is gone — `cd farm-frontend && pnpm lint` should run to completion (warn-level only; CI green); expect ~74 `max-lines` warnings.
- ⏳ Hetzner bucket end-to-end — bucket `farm-companion-blob-prod` (Helsinki / `hel1`) is created, but no S3 credentials generated, no env vars set in Coolify, no test upload through the adapter. Next operator step: Hetzner Console → Security → S3 credentials, capture key/secret (shown once), set the env vars from the ledger's Stage 0 milestone table.

## Open threads
- ⚠️ Branch confusion above — confirm CLAUDE.md + ledger changes are wanted on `chore/dependabot-triage-2026-05-17` and not on the original feature branch.
- ⚠️ Cwd drift — git was run from `farm-frontend/` at one point (paths showed `../CLAUDE.md`); minor, but future bash chains shouldn't assume repo root.
- ❌ Initial ESLint patch attempt via Edit tool failed the config-protection hook twice; do not retry without `ECC_DISABLED_HOOKS` set, or paste manually.

## Decisions
- **File-size thresholds 300/500/800 (source) and 600/1000/1600 (tests)**: tuned to actual codebase distribution (mean 281 LOC). Rejected single hard limit (no nuance for refactor-soon vs must-split-now) and stricter 200/400 (would warn on ~40% of files — noise floor too high).
- **ESLint `max-lines` set to `warn` at 500 only**: matches the hard tier. Rejected `error`-level (would block CI immediately on ~74 existing files) and warn-at-300 (197-warning floor would drown signal). Soft tier stays as a review-only nudge.
- **Rewrite `eslint.config.mjs` using direct flat-config imports** instead of pinning `@eslint/eslintrc` to an older version. The legacy bridge is what crashes; `eslint-config-next@16.1.3` ships native flat configs, so the bridge has no remaining purpose.
- **Hetzner bucket: Private + per-object public-read ACL** (matches adapter design — `produce/farm/county-blob.ts` all pass `access: 'public'`). Rejected Public (read-only) bucket — too permissive if anything private ever lands in the same bucket.

## You are here → next step
Decide whether to commit the CLAUDE.md + ledger changes on this branch, then apply the ESLint patch (paste from ledger, or relaunch Claude with `ECC_DISABLED_HOOKS=pre:edit-write:config-protection`) and run the three verification commands above.
