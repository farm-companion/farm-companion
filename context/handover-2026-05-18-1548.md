# Handover — 2026-05-18 15:48

> Branch: `fix/rate-limit-fail-closed` · 2 commits (pushed) · tracked tree CLEAN · 2 important untracked docs ⚠️

## Mission
User redirected mid-session from chasing rate-limit fixes ("bouncing pillar to post") to a proper full-site audit using superpowers + agents + council, with a comprehensive plan signed off **before** any further code changes.

## ⚠️ Untracked but important
Two durable deliverables sit on disk but are NOT in git:
- `docs/assistant/audit-2026-05-18.md` — the diagnosis (three-agent audit synthesis)
- `docs/assistant/migration-plan-2026-05-18.md` — the 12–14 slice plan (council-blessed)

Next session: decide whether to commit these into the architecture branch or leave them as session-only artifacts. They are **not** lost — they persist on disk. The tracked working tree itself is clean (`git status` shows only untracked notes/docs).

## What landed
- ✅ Rate-limit fail-closed migration off `Redis.fromEnv()` — `lib/rate-limit.ts`, `lib/rate-limit.test.ts`, `api/contact/submit/route.ts`, `api/farms/submit/route.ts`, ledger; commit `dcdb916` pushed
- ✅ Delete orphan `kv.lpush('farm-submissions:pending', id)` in farms/submit — `api/farms/submit/route.ts`, ledger; commit `599e653` pushed
- ✅ Architecture audit doc — `docs/assistant/audit-2026-05-18.md` **NOT YET COMMITTED**
- ✅ Migration plan doc — `docs/assistant/migration-plan-2026-05-18.md` **NOT YET COMMITTED**
- ❌ Slice B-followup-4 (delete dead `withPerformanceRateLimit`) — edited mid-session, reverted before commit when user redirected to audit-first

## Verified
- Rate-limit slices: `pnpm exec tsx --test src/lib/rate-limit.test.ts` → 9/9 ok 256ms; full per-file unit suite green (56 assertions); `tsc --noEmit` exit 0
- Operator confirmed Upstash KV cache writes are working in production (passed verification at session start)

## NOT verified — operator must do this
- ⏳ Open PR from `fix/rate-limit-fail-closed` against `master` (the two shipped commits) — no rebase, ship as-is per council unanimous verdict
- ⏳ Phase −0.5 production-verification queries (5 queries listed in `migration-plan-2026-05-18.md` "Phase −0.5") — `redis-cli KEYS 'farm-submission:*' | wc -l`, `HLEN 'farm_submissions'`, last-`Farm`-created Postgres query, workspace-wide `grep REDIS_URL`, `grep migrate-farms`. These validate the audit's premise before committing to 14 slices.
- ⏳ Answer open questions 2, 3, 5 in plan tail (Submission model shape, Google PhotoRef refresh path, PostGIS staging status)

## Open threads
- ❓ **Admin moderation may have been broken in production for months** — audit's static analysis says no farm can have been admin-approved since Era-1→Era-2 transition. Phase −0.5 Q3 (`SELECT ... FROM farms ORDER BY created_at DESC LIMIT 5`) confirms or refutes.
- ❓ Is the Coolify Redis container shared with `twitter-workflow` or `farm-pipeline`? Phase −0.5 Q4 grep answers; affects Phase 4 scope.
- ⚠️ `googlePhotoRef` URLs may already be expiring in production right now (Critic flag) — Phase 2 backfill must NOT copy expired URLs into Postgres as SSoT.
- ⚠️ Backfill is the single highest-risk artifact in the migration — Critic-mandated test harness (dry-run + idempotent UPSERT + count-validation gate) baked into Slice 1.4 and 2.4 designs.
- ❌ `pkill -f "tsx --test"` cleanup revealed a pre-existing bug: `performance-monitor.ts:64` `setInterval` keeps the cache-manager test process alive past assertion completion. Logged as Slice B-followup-3 candidate in plan. Not introduced by this session.

## Decisions
- **Audit-first instead of more slices**: user explicitly redirected mid-session ("we are bouncing pillar to post"). Rejected: continuing Slice B-followup-4 then -5 etc., which the user described as "chasing tails".
- **Submissions get write-then-flip cutover, not dual-write** (Skeptic-driven): the bug is read-side, not write-side; no live correct readers means no in-flight state to protect. Rejected: symmetric dual-write across submissions+photos (Architect's first position) — overkill for submissions.
- **Photos get dual-write Postgres-first** (Critic-driven): live readers exist on both sides; Postgres-first prevents drift if Redis write fails. Rejected: flag-flip single-atomic (Pragmatist alternative for photos) — too risky given signed-URL in-flight window up to 15 min.
- **No `@deprecated` quarantine of `database-constraints.ts`** (Skeptic): "decoration that confuses, doesn't delete". Either delete callers in Phase 0 or leave alone. Rejected: original audit recommendation to quarantine — theatre.
- **Ship rate-limit PR as-is, do not rebase into architecture branch** (unanimous council): correct on own merits, already pushed, fixes documented security regression. Rejected: fold into bigger plan — sunk-cost reasoning.

## Council deliberations
Convened on the 4-phase architecture plan. **Architect** (mine), **Skeptic**, **Pragmatist**, **Critic** subagents. Unanimous on Phase 0 standalone + ship existing PR as-is. Split on cutover policy resolved in favour of Skeptic's reframe (submissions write-then-flip, photos dual-write). Verdict compiled into `migration-plan-2026-05-18.md` header.

## You are here → next step
**Operator runs the 5 Phase −0.5 production queries from `docs/assistant/migration-plan-2026-05-18.md` and pastes results back — those answers gate Phase 0 kickoff. No code changes until then.**
