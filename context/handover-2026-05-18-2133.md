# Handover — 2026-05-18 21:33

> Branch: `master` · 15 commits this session (PRs #147–161, all merged) · 0 src files modified · No uncommitted code

## Mission
First-principles reset of farm-companion: deleted the prior audit's over-scoped plan, brainstormed a strip-then-build approach (hybrid Tool+Editorial spine), wrote spec + 10-slice Phase 0 plan, executed all 10 deletion slices, landed Phase 0.

## What landed
- ✅ Rate-limit fail-closed + orphan `kv.lpush` removal — `#147` (`a9add2e`)
- ✅ Audit + migration-plan + readiness docs — `#148` (`472c522`) [migration plan now superseded]
- ✅ Credential-handling hardening — `#149` (`5b3b0d7`)
- ✅ Reset design spec at `docs/superpowers/specs/2026-05-18-farm-companion-reset-design.md` — `#150` (`8692c2a`)
- ✅ Phase 0 implementation plan at `docs/superpowers/plans/2026-05-18-phase-0-strip.md` — `#151` (`5503cb0`)
- ✅ Phase 0 strip: 10 PRs `#152`–`#161` — ~50+ files deleted, several thousand LOC removed.
- ✅ Backup branch `backup/local-enrichment-pre-reset` preserves 4 unpushed local commits (farm-pipeline enrichment, 1,633 LOC) that pre-existed this session.

## Verified
- Phase 0 — each slice green: `pnpm exec tsc --noEmit`, `pnpm test:unit` 11/11, `pnpm build` all PASS; postflight greps zero hits. Vercel preview CI green on every PR pre-merge.
- Final controller-side check: `grep -r 'lib/redis|database-constraints|/api/admin/farms|/api/admin/migrate-farms|/api/photos/upload|/api/photos/finalize|/api/newsletter|/api/claims' farm-frontend/src` returns zero genuine hits (one false-positive substring in `lib/blob.ts:58`).
- Storage architecture now: Postgres canonical · Vercel KV ephemeral · Vercel Blob binaries. The audit's "phantom Coolify Redis" was wrong — only one Upstash backend behind two clients.

## NOT verified — operator must do this
- ⏳ Live deployed site post-strip not yet walked-through — verify by: open the Vercel production URL, visit `/`, `/shop`, `/shop/<known-slug>`, `/search?q=test`; confirm no broken images/pages from photo UI removal (Slice 0.5) or homepage edit (Slice 0.6).
- ⏳ Admin shell may have dead links to `/admin/farms`, `/admin/photos`, `/admin/claims` — verify by: log in to admin, click around; expect 404s on those routes (acceptable until Phase 3 rebuilds admin CRUD).

## Open threads
- ❓ Phase 1 brainstorming was just starting; visual-companion offer pending — operator said `/handover` first.
- ⚠️ `src/lib/blob.ts createUploadUrl()` returns dead `/api/photos/upload-blob` URL — zero callers in `src/`; Phase 1 cleanup.
- ⚠️ `src/lib/photos.ts` stubbed in Slice 0.10 (returns empty arrays) so `FarmPageClient`/`PhotoGalleryWrapper`/`FarmPhotoGallery` still compile. Public farm pages render empty galleries — correct for now; replace properly in Phase 3 admin photo CRUD or delete `lib/photos.ts` + the 3 consumers' empty-gallery code.
- ⚠️ `redis` npm package still in `farm-frontend/package.json` — 14 ops scripts in `farm-frontend/scripts/*.js` still import it. Phase 1 housekeeping: clean scripts then `pnpm remove redis`.
- 🚨 **Earlier in session the prod `REDIS_URL` (with password) was pasted in chat by accident.** Operator was advised to rotate. CONFIRM done: Coolify reset Redis password → updated env on BOTH `farm-frontend` AND `farm-produce-images` Vercel projects → redeployed both. Rotation protocol now documented in the spec, but session memory still contains the leaked credential.

## Decisions
- **Approach A: strip-then-build, not fix-in-place.** Pre-launch = zero user impact from aggressive deletion. Rejected B (fix-in-place keeps cognitive tax) and C (greenfield wastes Postgres schema, Maplibre, design tokens, rate-limit work).
- **Hybrid spine, Tool first.** Citymapper-style dominant lens; Editorial garnish Phase 4; Trip Planner deferred post-launch. Rejected pure-Editorial and pure-Trip-Planner.
- **KILL user-write features**, don't defer. Submissions, photo uploads, moderation, newsletter, claims all deleted now; rebuild Phase 3 operator CRUD when actually needed.
- **Ten slices for Phase 0**, not the audit's "5" — accurate file inventory required 10 to respect CLAUDE.md's 8-file budget.
- **Audit (`docs/assistant/audit-2026-05-18.md`) was wrong about Coolify Redis** — no separate container exists; `REDIS_URL` in `farm-frontend` points at the same Vercel KV (`farm-photos-kv` Upstash) that `@upstash/redis` uses. Reset spec owns this correction; original audit kept in history.

## You are here → next step
Restart Phase 1 brainstorming: re-invoke `superpowers:brainstorming`; operator replies yes/no to the visual-companion offer; then pick which Phase 1 weakness to anchor on (perf / map UX / data depth / search / visual polish / "audit it with me").
