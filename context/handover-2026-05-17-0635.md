# Handover — 2026-05-17 06:35

> Branch: `claude/add-sitemap-page-wHEV4` · 6 commits this session, all pushed · 0 uncommitted changes

## Mission
Pick up at "Run Slice 6c" from prior handover and drive the Stage 0 Vercel-SDK strip to completion, then land EV-1 email verification.

## What landed
- ✅ Slice 6c — 5 API routes migrated to `@/lib/kv`; `@vercel/kv` dropped from `package.json`. Commit `00e2518`.
- ✅ Slice 6d/6e spec — `@vercel/blob` adapter design + queued sub-slices. Commit `64a1592`.
- ✅ Slice 6d — new `farm-frontend/src/lib/blob-adapter.ts` (S3-compatible + fs backends behind `BLOB_BACKEND` env); 6 lib callers migrated; `@aws-sdk/client-s3` added. Commit `dca151f`.
- ✅ Slice 6e — 4 API routes migrated; `@vercel/blob` dropped. **Stage 0 Vercel-SDK strip complete.** Commit `fa32390`.
- ✅ Slice 6f — adapter hardened: `LegacyBlobUrlError` for `*.public.blob.vercel-storage.com` URLs, `BlobOptionNotImplementedError` for `allowOverwrite: false`, lazy env reads, `/blob/` URL round-trip fix (real bug caught by the new test on first run). First test file in the codebase: `src/lib/blob-adapter.test.ts` (11 cases, `node:test` + `tsx --test`). New `pnpm test:unit` script. Commit `a8a116b`.
- ✅ Slice EV-1 — `farm-frontend/src/lib/email-verification.ts` mailboxlayer adapter (fail-open semantics, 1000-entry LRU, AbortController timeout); wired into `api/contact/submit` + `api/farms/submit`; 14 unit tests; `.env.example` template (with `!.env.example` negation in both gitignores). 0 new deps. Commit `c278b5f`.

## Verified
- `pnpm exec tsc --noEmit` → exit 0 after every slice.
- `pnpm test:unit` → **25 pass / 0 fail** (11 blob-adapter + 14 email-verification).
- `pnpm build` (Next.js production) → exit 0 after Slice 6e and after EV-1; 99 static pages + all dynamic routes register.
- `grep '@vercel/*' farm-frontend/src` → only adapter header comments. Lockfile `@vercel/blob` + `@vercel/kv` count = 0.

## NOT verified — user must do this
- ⏳ Real Hetzner Object Storage round-trip — set `BLOB_BACKEND=s3` + `S3_*` envs in Coolify, upload a photo through `/api/photos/upload-blob`, confirm the returned URL is fetchable. Adapter is unit-tested but not exercised against live S3.
- ⏳ mailboxlayer integration — `echo 'MAILBOXLAYER_API_KEY=<key>' >> farm-frontend/.env.local && pnpm dev`, then run the three curl checks from `docs/assistant/email-verification-plan.md` §9 (golden / typo / disposable).
- ⏳ Coolify container build with the new envs — adapters are lazy so missing envs don't break startup, but the cutover hasn't been exercised end-to-end.
- ⏳ Open a PR — branch is 33 commits ahead of `master`; no PR yet.

## Open threads
- ⚠️ 45 Dependabot vulns on master (2 critical, 27 high) — +2 this session from `@aws-sdk/client-s3` transitives. Queue 1 still untouched.
- ⚠️ Legacy `*.public.blob.vercel-storage.com` URLs in the photos DB will throw `LegacyBlobUrlError` on `del()` after Coolify cutover (loud-by-design). Caller try/catch logs them; backfill is a separate operations task tracked in the ledger.
- ⚠️ Untracked `docs/PRODUCTION_READINESS_REPORT.md` carried over from previous session (ownership unclear).

## Decisions
- **Hetzner Object Storage** for prod blob backend; chosen from 4 options via user prompt. Uses `@aws-sdk/client-s3` for S3 compatibility. Rejected: Cloudflare R2 (extra provider), MinIO (operator owns availability), filesystem-only (single-host).
- **Split blob migration into Slice 6d + 6e** to respect the 8-file/slice cap. Rejected: single 10-file slice.
- **Throw on legacy Vercel URLs** rather than silently no-op'ing `del()`. Rejected: silent skip (would have orphaned Vercel-Blob objects after cutover).
- **Throw on `allowOverwrite: false`** rather than faking partial support. Rejected: TOCTOU HEAD-then-PUT race.
- **Skipped TDD on Slice 6d**, recovered with Slice 6f hardening pass that wrote the test suite *and* caught a real `/blob/` URL round-trip bug on first run — vindicating the recovery rather than papering over the gap.
- **Adopt `context/` for handovers** (this session's close-out): switch from rolling 3-deep `HANDOVER.md` to per-session dated files in `context/`. Cadence is too high (4 handovers in 7h) for the 3-cap to do anything but throw away load-bearing context. Mirrors existing `docs/assistant/` convention for ephemeral plans. Rejected: keep `HANDOVER.md` rolling (drops Hetzner provisioning details every few sessions).

## You are here → next step
Open a PR for `claude/add-sitemap-page-wHEV4` (Stage 0 + EV-1 ready for review) OR triage the 45 Dependabot vulns on master before adding more code to the diff.
